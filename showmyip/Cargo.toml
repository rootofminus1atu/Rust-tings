[package]
name = "showmyip"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
prettytable-rs = "0.10.0"
# proj = { version = "0.30.0", features = ["geo-types"] }